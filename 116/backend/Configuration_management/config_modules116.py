# config_modules.py

**Purpose**: Config

**Functions**: parse_filtered_values, strip_value, find_index_from_id, ensure_clean_directory, update_and_save_config_module and 1 more

**Dependencies**: json, os, pandas, importlib.util, sys and 2 more

**Keywords**: import, json, pandas, importlib, util, sys, copy

## Key Code Sections

### Imports

```
import json
import os
import pandas as pd
import importlib.util
import sys
# ...and 2 more imports
```

### Function: parse_filtered_values

```
def parse_filtered_values(filtered_values):
    """
    Parse a filtered_values string into a Python dictionary.

    This function converts a JSON string representation of filtered values into
    # ... more lines ...
```

### Function: strip_value

```
def strip_value(value):
    """
    Strip quotes from numeric values and convert to appropriate types.

    This function attempts to convert string values to their appropriate
    # ... more lines ...
```

## File Info

- **Size**: 17.5 KB
- **Lines**: 370
- **Complexity**: 18
